/**
 * @param { number } score
 * @returns { 'Unsatisfactory' | 'Satisfactory' | 'Hournor' }
 */

// ให้ สร้ าง Function เพื่ อหาคำตอบจากค่ าตั วเลข โดย...
// ถ้ า input หาร 3 ลงตั ว ให้ คืนค่ า "fizz"
// ถ้ า input หาร 5 ลงตั ว ให้ คืนค่ า "buzz"
// ถ้ า input หารทั้ ง 3 และ 5 ลงตั ว ให้ คืนค่ า "fizzbuzz"
// ถ้ า input หารทั้ ง 3 และ 5 ไม่ ลงตั ว ให้ คืนค่ าเป็ นตั วเลขเดิม


function calculateGrade(score) {

}

module.exports = calculateGrade